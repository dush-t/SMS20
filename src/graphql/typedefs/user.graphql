type User {
    _id: String!
    name: String
    email: String
    bits_id: String
    balance: Float
    stocksOwned: [StockOwned]
    stockTransactions: [StockTransaction]
}

type Query {
    user(_id: String!): User
}