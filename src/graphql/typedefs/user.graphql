type User {
    name: String
    email: String
    bits_id: String
    balance: Float
    # stocksOwned: [StockOwned]
    stockTransactions: [StockTransaction]
}

type Query {
    user(id: String!): User
}