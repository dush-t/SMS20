type Market {
  _id: String
  name: String
  exchangeRate: String
  priceRateChangeFactor: String
  is_active: String
  stocks: [Stock]
}

type Query {
  market(_id: String, name: String): Market
  markets: [Market]
}
